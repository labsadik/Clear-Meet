import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { ArrowLeft, LogOut } from "lucide-react";
import { useAuth } from "@/lib/auth-context";
export const Route=createFileRoute("/settings")({component:SettingsPage});
function SettingsPage(){const{user,signOut,loading}=useAuth();const navigate=useNavigate();if(loading||!user)return null;return <div className="page"><header className="nav"><Link to="/dashboard" className="brand"><span className="logo">C</span>Clear Meet</Link><Link className="button secondary" to="/dashboard"><ArrowLeft size={15}/>Dashboard</Link></header><main className="container" style={{paddingTop:42}}><div className="card"><div className="eyebrow">SETTINGS</div><h1>Account settings</h1><p className="muted">Manage your account and profile.</p><div className="actions"><Link className="button primary" to="/profile">Edit profile</Link><button className="button secondary" onClick={()=>void signOut().then(()=>navigate({to:"/"}))}><LogOut size={16}/>Sign out</button></div><div style={{marginTop:24,paddingTop:20,borderTop:"1px solid #eef0f4"}}><div className="meeting-meta">Signed in as</div><div style={{fontWeight:700,marginTop:4}}>{user.email}</div></div></div></main></div>}
